# [ archnon-project ]
[!] Automation/Script of tor as a secure proxy on systemd based linux distros.

[!] Use: git clone https://github.com/NewMaster27/Archnon-project && cd Archnon-project && mv archnon.git archnon && mv archnon /usr/bin && archnon help
